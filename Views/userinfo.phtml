<?php
// 这是一个视图文件，用于展示用户资料。

// Placeholder variables for testing without running the full controller/model (REMOVE IN PRODUCTION)
// 用于在没有运行完整控制器/模型时进行测试的占位符变量（在生产环境中移除）
if (!isset($name)) {
    $name = "Test User Full Name";
    $user_type = "Customer";
    $email = "test.user@tileplan.com";
    $phone = "+44 9876 543210";
    $address = "Example County, EX1 1XX";
    $member_since = "February 2024";
}
?>

<?php require ("Views/template/header.phtml");?>

    <div class="container userinfo-container">
        <div class="row justify-content-center align-items-center">

            <div class="col-md-4 text-center">
                <div class="profile-pic">
                    <i class="fas fa-user"></i>
                </div>

                <h4 class="mt-3"><?php echo htmlspecialchars($name); ?></h4>
                <p>dd/mm/yyyy</p>

                <p>User Type: <?php echo htmlspecialchars($user_type); ?></p>
            </div>

            <div class="col-md-1 d-flex justify-content-center">
                <div class="divider-line d-none d-md-block"></div>
            </div>

            <div class="col-md-6 mt-4 mt-md-0">
                <h4 class="text-center mb-3">Personal Information</h4>
                <div class="info-card">
                    <div>
                        <p><strong>Email:</strong> <?php echo htmlspecialchars($email); ?></p>

                        <p><strong>Phone:</strong> <?php echo htmlspecialchars($phone); ?></p>

                        <p><strong>Address:</strong> <?php echo htmlspecialchars($address); ?></p>

                        <p><strong>Member Since:</strong> <?php echo htmlspecialchars($member_since); ?></p>

                    </div>
                </div>
            </div>
        </div>

        <div class="row mt-5 text-center">
            <div class="col-md-4 mb-3">
                <button class="btn-darkblue home-btn">Home</button>
            </div>
            <div class="col-md-4 mb-3">
                <button class="btn-yellow change-password-btn">Change Password</button>
            </div>
            <div class="col-md-4 mb-3">
                <button class="btn-darkblue edit-profile-btn">Edit Profile</button>
            </div>
        </div>
    </div>

<?php require ("Views/template/footer.phtml");?>